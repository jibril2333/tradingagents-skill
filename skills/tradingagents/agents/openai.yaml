interface:
  display_name: "TradingAgents"
  short_description: "Multi-agent market research with subagents instead of LLM API calls"
  default_prompt: "Use $tradingagents to analyze NVDA for today and write the report in Chinese."
